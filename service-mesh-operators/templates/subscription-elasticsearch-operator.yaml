apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: elasticsearch-operator
  namespace: {{ .Values.subscriptions.elasticsearch.namespace }}
spec:
  channel: {{ .Values.subscriptions.elasticsearch.channel }}
  installPlanApproval: {{ .Values.subscriptions.elasticsearch.installPlanApproval }}
  name: elasticsearch-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
