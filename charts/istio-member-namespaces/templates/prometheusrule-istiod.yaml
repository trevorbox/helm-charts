{{- if .Values.userWorkloadAccess.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: istiod
  namespace: istio-system
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  {{- $.Files.Get "alerts/istiod.yaml" | nindent 2 }}
{{- end }}
